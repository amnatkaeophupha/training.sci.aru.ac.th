<?php $this->load->view('frontend/evaluation_header'); ?>
<div class="card eval-card"><div class="card-body p-4 p-md-5"><a href="<?= site_url('evaluation/'.$assignment->public_code) ?>" class="text-decoration-none">← กลับไปค้นหา</a><h1 class="h3 mt-3">ยืนยันว่าเป็นคุณ</h1><p><strong><?= html_escape($invitation->first_name.' '.$invitation->last_name) ?></strong></p>
<?php if($error): ?><div class="alert alert-danger"><?= html_escape($error) ?></div><?php endif; ?>
<?php $has_email=trim((string)$invitation->email)!==''; $has_phone=strlen(preg_replace('/\D+/','',(string)$invitation->phone))>=4; ?>
<?php if(!$has_email&&!$has_phone): ?><div class="alert alert-warning">ไม่มีข้อมูลอีเมลหรือโทรศัพท์ในระบบ กรุณาติดต่อเจ้าหน้าที่เพื่อแก้ไขก่อนทำแบบประเมิน</div><?php else: ?><form method="post"><p class="text-secondary">กรอกข้อมูลอย่างใดอย่างหนึ่งเพื่อยืนยันตัวตน</p><?php if($has_email): ?><label class="form-label">อีเมลเต็มที่ลงทะเบียน</label><input class="form-control form-control-lg mb-3" type="email" name="email" autocomplete="email"><?php endif; ?><?php if($has_email&&$has_phone): ?><div class="text-center text-secondary mb-3">หรือ</div><?php endif; ?><?php if($has_phone): ?><label class="form-label">เลขโทรศัพท์ 4 ตัวท้าย</label><input class="form-control form-control-lg mb-4" name="phone4" inputmode="numeric" pattern="[0-9]{4}" maxlength="4" autocomplete="off"><?php endif; ?><button class="btn btn-primary btn-lg w-100">ยืนยันตัวตน</button></form><?php endif; ?></div></div>
<?php $this->load->view('frontend/evaluation_footer'); ?>
